@page
@model ForgotPasswordModel
@{
    ViewData["Title"] = "Olvidé mi contraseña";
    Layout = "~/Views/Shared/_LoginLayout.cshtml";
}

<div class="auth-box overflow-hidden align-items-center d-flex">
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-xxl-4 col-md-6 col-sm-8 p-0">
                <div class="auth-brand text-center mb-4">
                    <a href="/" class="logo-dark">
                        <img src="~/images/loyola/logos/logo.png" alt="Logo oscuro" height="70">
                    </a>
                    <a href="/" class="logo-light">
                        <img src="~/images/loyola/logos/logo.png" alt="Logo claro" height="70">
                    </a>
                    <h4 class="fw-bold mt-3">Recupera tu contraseña</h4>
                    <p class="text-muted w-lg-75 mx-auto">Ingresa el correo electrónico asociado a tu cuenta y te enviaremos instrucciones para restablecer tu contraseña.</p>
                </div>

                <div class="card p-4 rounded-4">
                    <form method="post">
                        <div asp-validation-summary="ModelOnly" class="text-danger text-center mb-3" role="alert"></div>

                        <div class="mb-3">
                            <label asp-for="Input.Email" class="form-label">Correo electrónico <span class="text-danger">*</span></label>
                            <div class="input-group">
                                <input asp-for="Input.Email" class="form-control" placeholder="tucorreo@ejemplo.com" required />
                            </div>
                            <span asp-validation-for="Input.Email" class="text-danger"></span>
                        </div>

                        <div class="d-grid">
                            <button type="submit" class="btn btn-primary fw-semibold py-2">Enviar enlace de recuperación</button>
                        </div>
                    </form>

                    <p class="text-muted text-center mt-4 mb-0">
                        ¿Ya recordaste tus datos? <a asp-page="./Login" class="text-decoration-underline fw-semibold">Inicia sesión aquí</a>
                    </p>
                </div>

                <p class="text-center text-muted mt-4 mb-0">
                    <script>document.write(new Date().getFullYear())</script> ERP Loyola — por <span class="fw-semibold">Equipo de desarrollo</span>
                </p>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
